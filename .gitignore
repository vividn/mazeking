.pnpm-store/
node_modules/

# Environment variables
.env

# Logs and process files
*.log
*.pid

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Forge deployment artifacts — operator commits resulting addresses
# to frontend/src/lib/contracts.generated.ts after `just deploy-sepolia`.
contracts/deployments/

# Local anvil (chainId 31337) contract addresses for the frontend.
# Written by `just deploy-local`; never committed.
frontend/src/lib/contracts.local.ts
.runtime/
CLAUDE.local.md
